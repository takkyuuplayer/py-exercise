language: python
python:
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7-dev" # 3.7 development branch
before_install:
  - pip install --upgrade pipenv
  - pipenv --update
  - pip --version
  - pipenv --version
install:
  - make install
script:
  - make test
